# Backend handoff — стан робіт

> Робочий журнал реалізації бекенду. Оновлюється після кожного коміту.
> Гілка: `feat/backend-foundation` (від `development`).
> План: `C:\Users\olehc\.claude\plans\cozy-wishing-karp.md`

**Останнє оновлення:** 2026-09-07 — зріз 14 (Docker і спостережуваність).

---

## Прийняті рішення

Зафіксовані в ADR — читати їх, а не цей розділ, якщо потрібні деталі й
відхилені альтернативи.

| Рішення                                                                    | Де                                                                   |
| -------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| NestJS 11 + Drizzle + nestjs-zod, модульний моноліт, DDD-lite              | [ADR-0001](docs/adr/0001-nestjs-drizzle-modular-monolith.md)         |
| Два сервіси: `client-api` :3000 і `admin-api` :3001, без префікса `/admin` | [ADR-0002](docs/adr/0002-split-client-and-admin-api.md)              |
| Окремі `users` і `admins`, ланцюжки сесій на пристрій, deny-by-default     | [ADR-0003](docs/adr/0003-auth-model-tokens-and-admin-permissions.md) |
| Шляхи клієнтського API за REST-конвенціями, `/profile` замість `/users/me` | [ADR-0004](docs/adr/0004-client-api-url-conventions.md)              |
| Чого торкається видалення акаунту — **Proposed**, чекає на рішення         | [ADR-0005](docs/adr/0005-what-account-deletion-erases.md)            |
| Продукти поглинають інгредієнти; фільтри комбінуються по-різному в групах  | [ADR-0006](docs/adr/0006-products-absorb-ingredients.md)             |
| Добові норми — Mifflin-St Jeor; вода й кроки **чекають на підпис**         | [ADR-0007](docs/adr/0007-daily-norm-formulas.md)                     |

Додатково, поза ADR:

- База одна на обидва сервіси, через `packages/database` (`@dns/database`).
- Email — Resend; без `RESEND_API_KEY` вмикається stub-клієнт, що пише лист
  у лог. `OTP_DEV_CODE` фіксує код для локальної розробки.
- Патерни портуються з `D:\PhpstormProjects\11am-app` —
  `apps/mobile-api/CLAUDE.md` і `apps/mobile-api/.claude/skills/`.

### Словник шляхів клієнтського API

Вирішено 2026-09-06, зафіксовано в
[ADR-0004](docs/adr/0004-client-api-url-conventions.md): шляхи виводяться за
REST-конвенціями, а не успадковуються з V1. Ключове — колекції в множині,
синглтон поточного користувача це `/profile` (колекції `/users` у клієнтському
API немає), часткова зміна через один `PATCH` із частковим тілом, булевий стан
через `PUT`/`DELETE` підресурсу замість `toggle`, фільтри в query.

Виведена карта шляхів на всі домени —
[`.claude/knowledge/client-api-routes.md`](.claude/knowledge/client-api-routes.md).
Остаточний контракт кожного домену фіксує його `plan.md`.

`.claude/knowledge/**` (V1 на Supabase) лишається довідником **доменної
моделі** — таблиці, поля, енуми, формули — і не є контрактом URL.

---

## Зроблено

### Зріз 0 — ADR і вирівнювання доків ✅

| Коміт                                                        | Що                                                                                                                    |
| ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| `docs(adr): nestjs and drizzle modular monolith`             | ADR-0001                                                                                                              |
| `docs(adr): split client-api and admin-api`                  | ADR-0002                                                                                                              |
| `docs(adr): auth model, tokens and admin permissions`        | ADR-0003                                                                                                              |
| `docs: realign api topology to client-api and admin-api`     | 19 файлів: `docs/CLAUDE.md`, bucket- і домен-README, `docs/templates/plan.md`, root `CLAUDE.md`, `packages/CLAUDE.md` |
| `chore: env vars and workspace scripts for two api services` | `.env.example` переписано, скрипти `dev:client-api` / `dev:admin-api`                                                 |

ADR-0003 закрив сім `[NEEDS CLARIFICATION]` зі специфікацій `client/auth/*`:
TTL токенів (15 хв / 30 днів), реакція на повторне використання ротованого
токена, політика пароля, TTL коду (10 хв), ліміт спроб (5), пороги
rate-limit. Самі специфікації ще не оновлені — це робота зрізу 2 разом із
`plan.md`.

### Зріз 1 — фундамент ✅

| Коміт                                                                                  | Пакет                                 | Що                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `feat(shared-types): api envelope and pagination contract`                             | `@dns/shared-types`                   | `ApiResponse`, `PaginatedResponse`, `PaginationMeta/Query`, `ApiError`                                                                                                                               |
| `feat(constants): languages, measurement units and macro formulas`                     | `@dns/shared-types`, `@dns/constants` | енуми `Language` (20 локалей), `MeasurementUnit`, `MetricSystem`, `Macros`; `SUPPORTED_LANGUAGES`, `calculateCalories` (Atwater), 3 тести                                                            |
| `feat(database): drizzle connection module and base repository`                        | `@dns/database`                       | `DatabaseConnectionModule.forRootAsync`, `DATABASE_CONNECTION`, `BaseRepository`, `drizzle.config.ts`, власний раннер міграцій, заглушка сіду                                                        |
| `feat(api-common): exception filter, response interceptor, throttler guard and logger` | `@dns/api-common`                     | `GlobalExceptionFilter` (422 для Zod, прокидання `code`/`errors`, 500 без витоку), `ResponseInterceptor` (`{data}`), `CustomThrottlerGuard` + `@SetThrottleKey`, pino із редакцією секретів, 4 тести |
| `feat(api-infrastructure): email via resend with stub fallback, and otp codes`         | `@dns/api-infrastructure`             | `EmailModule` (Resend або stub залежно від `RESEND_API_KEY`), `OtpModule` (6 цифр через CSPRNG, bcrypt-хеш, `OTP_DEV_CODE`), 3 тести                                                                 |
| `feat(client-api): nestjs bootstrap with config, logger, swagger and health`           | `apps/client-api`                     | :3000, префікс `api/v1`, типізовані конфіги, глобальні pipe/filter/interceptor/guard, Swagger `/docs`, `GET /health`                                                                                 |
| `feat(admin-api): nestjs bootstrap with config, logger, swagger and health`            | `apps/admin-api`                      | :3001, те саме, власні `ADMIN_JWT_*`, коротший набір throttle-правил                                                                                                                                 |

---

### Зріз 2 — client auth ✅

| Коміт                                                                   | Що                                                                                      |
| ----------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `feat(database): users, refresh tokens, otp codes and oauth identities` | 5 таблиць + міграція `0000_thin_iceman.sql`, сутності, репозиторії                      |
| `feat(validation): auth schemas`                                        | `@dns/validation` з нуля; `AUTH_POLICY` у `@dns/constants`; 7 тестів                    |
| `fix(validation): otp pattern matched any six characters`               | регулярка приймала `dddddd` — бекслеш загубився при написанні файлу                     |
| `feat(client-api): sign-up with email verification code`                | register / verify-email / resend-code, `TokenService`, JWT-стратегія і guard            |
| `feat(client-api): sign-in with generic auth errors`                    | login, `GET /auth/me`                                                                   |
| `feat(client-api): refresh rotation, logout and logout everywhere`      | ротація ланцюжків, виявлення повтору                                                    |
| `feat(client-api): password reset with single-use permit`               | request → verify → complete                                                             |
| `feat(client-api): apple and google sign-in with create-or-link`        | `oauth/` в `api-infrastructure`, `OAuthSignInService`                                   |
| `fix(database): close the postgres pool on shutdown`                    | пул не закривався: тести не виходили, контейнер ігнорував SIGTERM                       |
| `test(client-api): auth flow specs against a real database`             | ts-jest + 3 db-специфікації, 17 тестів                                                  |
| `docs: plans for client auth and status sync`                           | 4 × `plan.md`, специфікації → `Implemented`, `.claude/knowledge/auth/client-auth-v2.md` |

**11 ендпоінтів** під `/api/v1/auth`: `register`, `verify-email`,
`resend-code`, `login`, `oauth`, `refresh`, `logout`, `logout-all`, `me`,
`password-reset/{request,verify,complete}`.

Дві речі, знайдені під час роботи і виправлені окремими комітами:

- **Permit-токен проходив би як access-токен.** `JwtStrategy` завантажувала
  користувача за `sub` і не дивилась на призначення токена, тож той, хто знає
  код відновлення, отримував би сесію — рівно те, що password-reset FR-009
  забороняє. Тепер у кожного токена явний claim `type`, який перевіряє кожен
  споживач, а permit підписано іншим ключем.
- **Ротація в 11am видаляє всі токени користувача**, тобто подовження на
  телефоні розлогінює планшет. Це суперечить session FR-006, тож у нас
  ротація торкається лише свого ланцюжка, а повтор спожитого токена вбиває
  один пристрій.

### Зріз 3 — client user ✅

| Коміт                                                                 | Що                                                                                                            |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `feat(database): profiles, settings, reminders and deletion requests` | 4 таблиці + міграція `0001_mighty_corsair.sql`; `UserRepository.createAccount` заповнює їх однією транзакцією |
| `feat(client-api): profile and app settings`                          | `GET`/`PATCH /profile`, `PATCH /profile/settings`                                                             |
| `feat(client-api): meal and weigh-in reminders`                       | `GET`/`PUT /profile/reminders`                                                                                |
| `feat(client-api): account deletion with a grace period`              | `POST`/`DELETE /profile/deletion-request`, `deletionScheduledFor` у `/auth/me`                                |
| `test(client-api): user domain specs`                                 | 13 db-тестів                                                                                                  |
| `docs: plans for client user and status sync`                         | 5 × `plan.md`, статуси специфікацій, карта шляхів                                                             |

**8 нових ендпоінтів** під `/profile`. Реєстрація тепер провізіонує акаунт
цілком: профіль, налаштування і пʼять нагадувань в одній транзакції з
`users` — і для реєстрації паролем, і для входу через провайдера.

Три рішення, які варто знати:

- **Профіль і налаштування — окремі таблиці, не ширші `users`.** `JwtStrategy`
  читає `users` на кожен авторизований запит, і цей рядок має лишатися вузьким.
- **Запит на видалення не обриває сесію.** Відновлення — авторизований виклик,
  тож інакше користувач опинився б на екрані відновлення без змоги ним
  скористатися.
- **`GET /profile/settings` навмисно немає.** Налаштування приходять вкладеними
  в `GET /profile`; два шляхи читання тих самих даних розходяться першими.

Специфікації `profile`, `profile-edit` і `account-deletion` мають статус
`Approved`, а не `Implemented` — у кожній лишилась частина, заблокована іншим
доменом; що саме, написано в розділі «Що ще не побудовано» їхніх `plan.md`.

---

### Зріз 4 — сховище файлів ✅

| Коміт                                                                              | Що                                                                 |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `feat(api-infrastructure): s3 storage with presigned uploads and ownership checks` | `StorageModule`, `validateOwnership`, 8 тестів                     |
| `feat(client-api): presigned uploads and profile photo`                            | `POST /uploads`, `photoUrl` у `PATCH /profile`                     |
| `feat(client-api): support tickets with attachments`                               | `POST /profile/feedback`, таблиця `feedback`                       |
| `fix(client-api): ownership checks must reject, not throw synchronously`           | метод типізовано як `Promise`, а кидав синхронно — тест це знайшов |
| `docs: deletion must clear support reply addresses explicitly`                     | знайдена й закрита прогалина приватності                           |

Байти йдуть **повз API** — клієнт бере підписаний дозвіл і вивантажує напряму
в сховище. Перевірено наскрізно на MinIO: presign → PUT → GET, байти
ідентичні.

Дві речі, які виявилися лише під час перевірки:

- **Розмір вшивається в підпис.** Оголосив 1024, вивантажив 160 — MinIO віддає
  непрозорий 403. Клієнт мусить називати точний розмір; зафіксовано в
  `apps/client-api/CLAUDE.md`.
- **`ON DELETE SET NULL` не чистить email.** Тікет переживає видалення автора
  (як і задумано ADR-0005), але `reply_email` лишався з особистою адресою.
  Відтворено, виправлено кроком у runbook, покрито тестом.

### Зріз 5 — client nutrition ✅

| Коміт                                                                         | Що                                              |
| ----------------------------------------------------------------------------- | ----------------------------------------------- |
| `docs(adr): products absorb ingredients, filter combination semantics`        | ADR-0006                                        |
| `feat(database): nutrition goals, meal, water and step logs`                  | 4 таблиці + міграція `0003_dry_lady_ursula.sql` |
| `feat(validation): nutrition goal, meal, water and step schemas`              | 11 тестів                                       |
| `feat(client-api): nutrition goal, daily slice, meal, water and step logging` | 8 ендпоінтів                                    |
| `test(client-api): nutrition domain specs`                                    | 14 db-тестів                                    |
| `docs: plans for client nutrition and status sync`                            | 3 × `plan.md`                                   |

Три рішення, які варто знати:

- **Денний підсумок не зберігається, а рахується запитом.** V1 тримав
  матеріалізовану `daily_nutrition_summary`, яку мав наповнювати тригер —
  тригер так і не написали, і таблиця назавжди показувала нулі.
- **Записаний прийом їжі — зліпок, а не посилання.** Рецепт можна
  відредагувати, а чек має лишатися чеком. Це ж рішення дозволило зробити
  домен **до** появи каталогу рецептів: `recipe_id` nullable і поки без FK.
- **Вода — журнал, кроки — одне значення.** Дзеркально і навмисно: склянку
  треба вміти забрати, а кроки приходять уже підсумованими, і додавання
  подвоїло б їх на другій синхронізації за день.

### Зріз 6 — client onboarding ✅

| Коміт                                                                          | Що                                                                                     |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- |
| `feat(constants): daily norm formulas from the questionnaire`                  | ADR-0007 + чисті функції, 13 тестів                                                    |
| `feat(database): questionnaire fields on profiles and recommendation snapshot` | 9 колонок на `profiles`, 3 на `nutrition_goals`, міграція `0004_overjoyed_gateway.sql` |
| `feat(client-api): onboarding questionnaire and daily recommendations`         | 4 ендпоінти під `/profile`                                                             |
| `test(client-api): onboarding questionnaire specs`                             | 12 db-тестів                                                                           |
| `docs: plan for onboarding and status sync`                                    | `plan.md`, статус специфікації                                                         |

Три речі варті уваги:

- **Формула була відкритим питанням, і я його закрив, а не обійшов.**
  Специфікація прямо каже, що жодна норма не рахується на клієнті, тож без
  формули екрани 14–16 показували б однакову константу для 50-кілограмової
  жінки і 110-кілограмового чоловіка. Взяв Mifflin-St Jeor — але статус
  ADR-0007 `Proposed`, бо норми води й кроків я вибрав інженерно, а не як
  фахівець.
- **Завершення анкети пише і прапорець, і ціль харчування — однією дією.**
  Кроки 14–16 і є екраном цілі в іншому вбранні; акаунт, позначений
  завершеним, але без цілі, викинув би людину на трекінг без кілець.
- **Рекомендація зберігається зліпком поруч із вибором.** Специфікація
  вимагає бачити, наскільки користувач відхилився, а свіжа рекомендація
  зсувається разом із вагою — без зліпка порівнювати було б із рухомою
  мішенню.

Рекомендація повертає `null`, доки анкета не зібрала все потрібне. Не
дефолти: норма, порахована з вигаданої ваги, виглядає так само авторитетно,
як справжня.

### Зріз 7 — client progress ✅

| Коміт                                                                         | Що                                                        |
| ----------------------------------------------------------------------------- | --------------------------------------------------------- |
| `feat(database): body measurements for weight, waist and height`              | таблиця + репозиторій, міграція `0005_ambiguous_blob.sql` |
| `feat(validation): progress window and measurement schemas`                   | вікно `?days=`, показник у шляху, вимір                   |
| `feat(client-api): progress metrics overview, detail and measurement logging` | 4 ендпоінти під `/progress`                               |
| `feat(client-api): patch a single nutrition target instead of the whole goal` | `PATCH /nutrition/goal`                                   |
| `feat(client-api): target weight is editable from the profile`                | `targetWeightKg` у `PATCH /profile`                       |
| `test(client-api): progress db specs and partial goal edits`                  | 19 + 2 db-тести                                           |
| `docs: plans for client progress and status sync`                             | 3 × `plan.md`, статуси, карта шляхів                      |

Три речі варті уваги:

- **Половина домену не має власного сховища.** З шести показників екрана
  калорії, вода і кроки вже належать `nutrition` — прогрес їх читає. Своя
  таблиця на всі шість дала б одному числу два джерела, і перший же розбіг
  між денним екраном і графіком нікому не вдалося б пояснити. Нових таблиць
  тут одна: `body_measurements` на вагу, талію і зріст.
- **Читати можна девʼять показників, писати — три.** Калорії, воду і кроки
  пишуть через `/nutrition`, де день є частиною шляху. Другий шлях запису в
  те саме число дозволив би логу і графіку розійтися, і сказати, який із них
  правий, було б нічим.
- **Цілі лишились у доменів, які ними володіють.** Карта шляхів передбачала
  `PUT /progress/metrics/{metric}/goal` — я його не побудував: щоденні цілі
  міняє `PATCH /nutrition/goal`, цільову вагу — `PATCH /profile`. Фасад під
  `/progress` дав би одній колонці двох власників. `PATCH` до цілі свідомо
  не створює її: народити ціль із одного поля означало б вигадати шість
  інших.

Запис ваги і зросту додатково оновлює профіль — інакше «оновлена норма» після
зважування рахувалася б із ваги, яку людина замінила тижні тому.

### Зріз 8 — client recipe + product ✅

| Коміт                                                                | Що                                                                    |
| -------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `feat(database): products, recipes and the filter dictionaries`      | 17 таблиць + міграція `0006_colossal_khan.sql` із довідниковими рядками |
| `feat(validation): catalog query and custom product schemas`         | 11 тестів                                                              |
| `feat(client-api): recipe catalog, filters, search and product lookup` | 8 ендпоінтів під `/recipes` і `/products`                             |
| `test(client-api): catalog db specs`                                 | 25 db-тестів                                                           |
| `docs: plans for client recipe catalog and status sync`              | 4 × `plan.md`, статуси, карта шляхів                                   |

Рішення, ухвалені перед першою таблицею (питання були відкритими):

- **Мультимовність лишається як у V1** — `*_translations` на продукти,
  рецепти, кроки і всі чотири довідники. 272 продукти USDA вже мають 20
  перекладів, адмінка вже їх редагує, а вкрутити i18n пізніше означало б
  переписати кожен запит читання. Читання йде за `user_settings.language`
  з фолбеком на українську, потім на `slug` — зникнути з каталогу гірше,
  ніж показатися чужою мовою.
- **Кухня — рівно одна, колонкою** (закрито відкрите питання ADR-0006).
  Деталь називає одну, форма створення дає обрати одну, а множинний вибір
  у фільтрі — це OR, який колонка обслуговує через `IN (…)`.
- **Довідники — таблиці в БД, засіяні міграцією.** Рецепт посилається на
  рядок, і таких рецептів тисячі; дванадцята категорія не має вимагати
  релізу двох застосунків. Разом із ними приїжджають 15 «швидких»
  продуктів, яких `recipe-filters` FR-002 називає поіменно.

Три речі варті уваги:

- **КБЖВ рецепта зберігається, а не рахується зі складу на читанні.**
  Каталожні рецепти приходять із авторськими числами, і частина їхніх
  інгредієнтів досі без макросів (відкрите питання ADR-0006) — виведення
  на читанні тихо занизило б саме ці страви. Власна страва отримає ті самі
  колонки, заповнені сервером **під час запису**.
- **Числа описують усю страву; на порцію — це `total / servings`.** Так
  рахує і картка, і фільтр калорійності. Верх повзунка (800) означає
  «800+», тож умова просто не додається.
- **`/recipes/count` не побудовано навмисно.** Живий лічильник екрана
  фільтрів — це `meta.total` звичайного `GET /recipes`; окремий маршрут дав
  би числу під кнопкою шанс розійтися зі списком за нею. Так само немає
  `/ingredients`: інгредієнт — це продукт (ADR-0006).

Склад рецепта — **тільки в грамах**. Колонка `unit` з V1 пустила б у
таблицю «1 склянку» — рядок, макроси якого ніхто не порахує, всередині
страви, підсумки якої мають зійтися.

### Зріз 9 — client create-dish ✅

| Коміт                                                     | Що                                            |
| ----------------------------------------------------------- | ----------------------------------------------- |
| `feat(database): step ingredients and own-dish writes`    | таблиця `recipe_step_ingredients`, міграція `0007_striped_hydra.sql`, `createOwn`/`deleteOwn` |
| `feat(validation): create dish schema`                    | 5 тестів                                       |
| `feat(client-api): create and delete an own dish`         | `POST /recipes`, `DELETE /recipes/{id}`        |
| `test(client-api): create-dish specs`                     | 13 db-тестів                                   |
| `docs: plan for create-dish and status sync`              | `plan.md`, статус, карта шляхів                |

Рішення, ухвалені перед кодом (питання були відкритими в специфікації):

- **Щонайменше один інгредієнт, вага > 0.** Страва без складу має КБЖВ = 0,
  потрапляє в журнал і тихо занижує день: людина бачить «зʼїв обід» і нуль
  калорій. Відмова при збереженні чесніша за нуль у підсумку.
- **Видалення власної страви є, редагування — немає.** Специфікація явно
  виносить `PATCH` за межі; без `DELETE` таб «Власні» лише ріс би.
- **Порції, вага і час виводяться, а не питаються.** Форма (FR-001) не
  збирає жодного з них: одна порція, вага = сума складу, час = сума
  таймерів кроків. Опційне поле під число, якого не збирає жоден екран,
  було б другим джерелом для нього.

Три речі варті уваги:

- **КБЖВ рахується один раз, під час запису.** Перерахунок на читанні
  переписував би історію при першому ж виправленні даних продукту, і вже
  записаний прийом їжі перестав би збігатися з чеком, який людині показали.
- **Калорії — з власного числа продукту, не з Atwater по трьох макросах.**
  Дані USDA враховують клітковину й цукрові спирти, яких `4/4/9` не бачить,
  а рядки складу на екрані показують саме це число — тож підсумок страви
  дорівнює сумі рядків під ним.
- **Чипси кроку приходять як позиції у списку складу**, не як id: клієнт
  створює ці рядки цим же запитом. Позиція за межами списку — 400.

Крок може не мати жодного слова — редактор вважає його заповненим, щойно є
хоч щось, а номер і тривалість живуть на самому кроці. Такий крок просто не
отримує рядка перекладу.

### Зріз 10 — client meal-plan ✅

| Коміт                                                                     | Що                                                            |
| --------------------------------------------------------------------------- | --------------------------------------------------------------- |
| `feat(database): the meal plan, and a date validator that actually rejects Feb 31` | `meal_plan_items`, міграція `0008_mean_peter_quill.sql` |
| `feat(client-api): the weekly meal plan`                                  | 5 ендпоінтів під `/meal-plan`                                  |
| `test(client-api): meal plan specs`                                       | 16 db-тестів + 7 тестів схем                                   |
| `docs: plan for the meal plan and status sync`                            | `plan.md`, статус, карта шляхів                                |

Рішення, ухвалені перед кодом:

- **Копіювання перезаписує цільовий день.** «Копіювати» означає, що ціль
  стає такою ж, як джерело; доповнення на вже запланованому дні тихо
  подвоїло б калорії, а шторка відкоту не має. Порожній день копіювати
  заборонено — це очистило б кожну ціль.
- **Позиція плану — лише страва, не продукт.** Вкладка «Інгредієнти» в
  пікері не має контракту порції (відкрите питання специфікації); без
  відповіді на «скільки грамів за замовчуванням» калорійність була б
  вигаданою.
- **FR-006 (список покупок) відкладено** до зрізу `shopping-list`. SC-003
  вимагає, щоб стан «у списку» виводився зі списку, а не дублювався
  прапорцем у плані — тож будувати його наперед нема з чого.

Три речі варті уваги:

- **Позиція плану — посилання, а не зліпок; це протилежність журналу
  харчування.** Запис журналу — чек про зʼїдене і копіює числа; позиція
  плану — намір щодо страви, яка ще існує. Звідси зовнішній ключ і каскад:
  видалили страву — готувати нема чого.
- **`?from=&to=`, а не `?week=`.** Де починається тиждень — справа клієнта;
  сервер інакше вгадував би перший день тижня локалі й для когось помилявся.
- **Стан дня рахує сервер тим самим допуском ±10 %**, яким прогрес фарбує
  день (ADR-0007). Два визначення «приблизно в нормі» дозволили б вкладці
  плану і графіку прогресу розійтися про ту саму добу.

Принагідно виправлено давню ваду валідації: `Date.parse` для рядка, який
ISO-граматика відкидає, вмикає легасі-парсер, тож `2026-02-31` повертався як
3 березня замість `NaN` — і `logDateSchema` таку дату приймала. Стосувалося
всіх доменів, що беруть дату від клієнта.

### Зріз 11 — client shopping-list ✅

| Коміт                                                                          | Що                                                      |
| -------------------------------------------------------------------------------- | --------------------------------------------------------- |
| `feat(database): the shopping list, and one product taxonomy for two screens`  | 3 таблиці + 2 групи, міграція `0009_modern_blonde_phantom.sql` |
| `feat(client-api): the weekly shopping list`                                   | 8 ендпоінтів під `/shopping-list`                        |
| `test(client-api): shopping list specs`                                        | 18 db-тестів + 5 тестів схем                             |
| `docs: plans for the shopping list and status sync`                            | 2 × `plan.md`, статуси, карта шляхів                     |

Рішення, ухвалені перед кодом:

- **Одна таксономія продуктів на два екрани.** Фільтр рецептів пропонує
  шість груп, список покупок групує по семи, з яких двох фільтр не бачив.
  Друга таксономія означала б, що хтось тримає обидві в узгодженні для 272
  продуктів — і вони розійдуться. Тож у `product_groups` додано «Мучні» й
  «Крупи», прапорець `is_recipe_filter` і **другий стовпчик порядку**:
  фільтр починає з овочів, список — із мʼясного.
- **Імпортовані позиції не зберігаються, а виводяться з плану на кожному
  читанні.** Відкрите питання про «правила синхронізації» відповіді не
  потребує: нічого не копіювалося, тож нічому й розходитися.
- **Список не чиститься сам ніколи.** Автоматичне очищення на межі тижня
  вимагало б від сервера рішення, де тиждень починається, і забирало б
  список у неділю ввечері в того, хто ще не сходив у магазин. Є явне
  очищення і видалення позиції — обидва випереджають макет.

Три речі варті уваги:

- **Відмітки живуть окремо від позицій**, бо в імпортованого рядка немає
  рядка. Ключ включає походження: той самий продукт може стояти в списку
  двічі, і купівля ваших 200 г не має гасити 500 г, потрібних плану. FR-005
  (вимкнув перемикач — відмітки на місці) виходить із цього сам собою.
- **Кількість приходить одиницею й числом, не грамами.** Клієнт помножив би
  не гірше, але «порція — 250 г» замерзло б у релізі, а специфікація має
  відкрите питання про пер-продуктні еквіваленти.
- **Значення понад максимум відхиляється, а не обрізається.** Специфікація
  пропонує обрізати; тихо змінити введене людиною число гірше за відмову.

### Зріз 12 — client notifications + FAQ ✅

| Коміт                                                        | Що                                                |
| -------------------------------------------------------------- | --------------------------------------------------- |
| `feat(database): notifications and editorial FAQ content`    | 5 таблиць, міграція `0010_calm_boomer.sql` із сідом |
| `feat(client-api): the notification inbox and the FAQ`       | 5 ендпоінтів                                       |
| `test(client-api): notification and FAQ specs`               | 14 db-тестів                                       |
| `docs: plans for notifications and FAQ`                      | 2 × `plan.md`, статуси, карта шляхів               |

Рішення, ухвалені перед кодом (усі три були відкритими питаннями):

- **Групує за днями клієнт, не сервер.** До якого дня належить мітка часу,
  залежить від часового поясу пристрою — те саме правило, за яким журнал
  харчування отримує дату від клієнта.
- **Детального маршруту немає.** Список віддає повідомлення цілком; різниця
  між рядком і карткою в тому, скільки з одного тексту кожен малює.
  Обрізання до двох рядків робить клієнт — лише він знає свою ширину.
- **Пагінація — offset скрізь** (`page`/`limit` з `meta`). Курсор мав би сенс
  для стрічки, що росте зверху; тут списки гортають сторінками.

Дві речі варті уваги:

- **Сповіщення зберігає свій текст, а не шаблон.** Це запис про вже
  надіслане — у випадку push формулювання пішло з будинку в мить доставки, і
  відтворення з шаблона дозволило б інбоксу й push-у казати різне про ту саму
  подію. Це ж вирішує мову: написане тією, якою акаунт читав, у ній і
  лишається.
- **FAQ засіяно справжньою копією застосунку** — сім тем і сорок одне питання
  з `faq.constants.ts`, де стояв `TODO: replace with the FAQ endpoint`.
  Контент перенесено, а не вигадано: вигадувати продуктовий текст замість
  редакції гірше за порожній екран.

Специфікація сповіщень лишається `Approved`, і причина не в обсязі роботи:
**сповіщення нікому створювати**. Клієнтський API їх лише читає; джерела —
нагадування, події підписки, системні розсилки — живуть у доменах, яких ще
немає. `NotificationRepository.create` існує, щоб коли джерело зʼявиться,
воно писало рядок одним способом.

### Зріз 13 — client subscription + referral ✅

| Коміт                                                             | Що                                                     |
| ------------------------------------------------------------------- | -------------------------------------------------------- |
| `feat(database): subscription plans, receipts and referral codes` | 6 таблиць + `profiles.paywall_seen_at`, міграція `0011_milky_hammerhead.sql` |
| `feat(client-api): the paywall, purchases and referral codes`     | 7 ендпоінтів + `PurchasesModule` в `api-infrastructure` |
| `test(client-api): subscription specs`                            | 20 db-тестів                                            |
| `docs: plans for subscription and referral`                       | 2 × `plan.md`, статуси, карта шляхів                    |

Рішення, ухвалені перед кодом (усі три були відкритими питаннями):

- **Плани — таблиця в базі з id товару магазину.** SC-003 вимагає міняти
  ціни без релізу, а бейдж економії та «який план обраний за замовчуванням»
  магазин не знає взагалі. Що саме списується і в якій валюті — усе одно
  його справа.
- **Перевірка чека — шов зі stub**, як з OAuth. Потребує ключа App Store
  Connect і service-акаунта Google; код проти облікових даних, яких немає,
  неможливо запустити.
- **Код постійний, одне погашення на акаунт назавжди**, нагорода — місяць
  на місячному плані. Первинний ключ `referral_redemptions` — на тому, хто
  гасить: це і є правило, а не перевірка в сервісі.

**Дві вади знайшли тести, а не підтвердили:**

- Чек із періодом, що вже минув (відновлення простроченої підписки), валив
  сервіс у 500: він створював рядок і шукав його через `findActive`, фільтр
  якого цей рядок навмисно ховає. Тепер `create` повертає записане.
- Прострочений рядок, позначений `active`, **назавжди блокував би нову
  покупку** — база дозволяє один активний рядок на акаунт. Тепер такі рядки
  підмітаються в момент покупки, а не нічним завданням.

Сіди — справжні числа з макета
(`subscription.constants.ts`, де стояв `TODO: replace with the plans
endpoint`): рік $59.99 (повна $119.88, 7 днів пробного), місяць $9.99, пʼять
можливостей українською з файлу локалізації. Вигадувати ціни не довелося.

Специфікація лишається `Approved` з однієї причини: **перевірку чека проти
Apple і Google не побудовано**. І окремо варто памʼятати, що **підписка
зараз ні на що не впливає** — жоден ендпоінт не перевіряє її наявність, бо
специфікація не описує, що саме перестає працювати без неї.

### Зріз 14 — Docker і спостережуваність ✅

| Коміт                                                          | Що                                           |
| ---------------------------------------------------------------- | ---------------------------------------------- |
| `feat(client-api): prometheus metrics and a readiness probe`   | `/metrics`, `/health/ready`                   |
| `feat(infra): docker images and the observability stack`       | образ, два compose-стеки, конфіги, nginx      |

Побудовано за зразком `D:\PhpstormProjects\11am-app` (`infra/prod/`), з
трьома відмінностями під наш випадок:

- **Postgres поза Docker.** Стек його не створює і не видаляє; `DATABASE_URL`
  вказує, де він, а `extra_hosts: host.docker.internal` дає дотягнутися до
  бази на тому ж хості.
- **Redis немає взагалі** — його ніхто не читає.
- **Один параметризований Dockerfile** на обидва API замість двох копій: вони
  відрізняються лише іменем пакета, і друга копія розійшлася б із першою при
  першому ж виправленні.

Три речі варті уваги:

- **`service` — єдина мітка, яка тримає логи й метрики разом.** Prometheus
  скрейпить `client-api:3000` за іменем compose-сервісу, Promtail виводить
  мітку `service` з того самого імені. Перейменувати сервіс — тихо
  розʼєднати сплеск затримки й логи, що його пояснюють.
- **Гістограма пишеться в middleware, не в інтерсепторі.** Nest виконує
  middleware до guard'ів, тож інтерсептор не побачив би жодного 404, 401 від
  `JwtGuard` і 429 від throttler'а — саме тих відповідей, заради яких
  метрики й потрібні.
- **`/health` відповідає `ok` із мертвою базою.** Тому зʼявився
  `/health/ready`, який питає базу і віддає 503. Перевірено в контейнері:
  з непрацюючою базою health = 200, ready = 503.

Образ `client-api` — 460 МБ (`node:22-slim`); `pnpm deploy --prod` витягує
самодостатнє дерево замість hoisted-node_modules на весь монорепо.

**`/metrics` нічим не захищений** — Prometheus ходить внутрішньою мережею, і
публічний vhost мусить його закрити. Шаблон nginx це робить.

### Зріз 15 — алерти в Telegram ✅

| Коміт                                              | Що                                          |
| ---------------------------------------------------- | --------------------------------------------- |
| `feat(infra): telegram alerting for grafana`       | контактна точка, маршрути, 7 правил у теці `DNS` |

За зразком 11am, але правила свої — під наші метрики й під те, що база поза
Docker. Сім штук, три критичні: `dns-readiness-failing` (`/health/ready` не
200 — тобто база недосяжна, хоча процес живий), `dns-api-unscrapeable`
(Prometheus не дістає контейнера) і `dns-disk-low`. Решта — warning: 5xx понад
5%, p95 понад 1 с, памʼять під 10%, `admin-api` не відповідає.

Три речі, які коштували часу:

- **Chat id мусить лишатися inline у `contact-points.yml`.** Grafana
  підставляє `$VAR` **після** парсингу YAML і перетипує результат, тож
  відʼємний id групи приходить числом і валідація падає
  (`grafana/grafana#69950`, відкритий із 2023). Лапки не рятують — на момент
  підстановки їх уже немає. Токен через env працює: він не числовий.
- **Погана provisioning-конфігурація кладе весь старт Grafana**, а не один
  алерт. Тобто помилка в цих файлах — це «немає моніторингу», не «немає
  сповіщень». Тому конфіг прогнано в реальному контейнері перед комітом:
  7 правил, 1 контактна точка, дерево маршрутів і дашборд — усе в одній теці
  `DNS`.
- **`noDataState` важливіший за поріг.** Для доступності відсутність даних —
  це і є відмова (`Alerting`). Для 5xx і p95 — `OK`: `histogram_quantile` над
  порожнім діапазоном о 4:00 нічого не повертає, і алерт на це вчить мутити
  канал.

`parse_mode` свідомо не заданий: лог-рядок із `<`, `>` чи `_` інакше змусив би
Telegram відхилити все сповіщення як зламану розмітку.

Chat id групи (`-1003784473088`, та сама, що в 11am) закомічено в
`contact-points.yml` — без токена він марний, і бот усе одно мусить бути
учасником групи. Тож на сервері лишається одне: `TELEGRAM_BOT_TOKEN` у
`.env.obs`. Кроки — в `infra/prod/README.md`.

### Зріз 16 — порти під сервер ✅

| Коміт                                            | Що                                     |
| -------------------------------------------------- | ---------------------------------------- |
| `fix(infra): make service ports configurable`    | 3028/3029/3030/3031, порт з `.env` наскрізь |

Стандартні 3000/3001/3300 на сервері зайняті. Тепер: client-api 3028,
admin-api 3029, Grafana 3030, Prometheus 3031 — усе на `127.0.0.1`, бо nginx
стоїть на хості, не в контейнері.

- **`environment:` у compose перекриває `env_file:`.** Там був хардкод
  `CLIENT_API_PORT: '3000'`, тобто значення з `.env.prod` мовчки
  ігнорувалося б: застосунок слухав би 3000, а публікація віддавала б 3028.
  Прибрано — порт тепер приходить лише з `.env.prod`, одне число і всередині
  контейнера, і на хості.
- **Prometheus не підставляє змінні оточення в `static_configs`** — `${VAR}`
  він скрейпив би буквально. Порти в `prometheus.yml` хардкод, і це єдине
  місце, яке треба міняти разом із `.env.prod`. Поломка тиха: ціль стає
  `DOWN`, і через дві хвилини прилітає `dns-api-unscrapeable`, ніби сервіс
  упав.
- **Prometheus тепер опублікований** (`127.0.0.1:3031`), але свідомо без
  nginx-vhost: у нього немає жодної автентифікації, а admin-API вміє видаляти
  серії. Доступ — тунелем або через Grafana Explore.

Перевірено `docker compose config` на обох стеках: `127.0.0.1:3028→3028`,
`3029→3029`, `3030→3000`, `3031→9090`, і змінні доїжджають у контейнери.

---

### Зріз 17 — мок-рецепти ✅

| Коміт                                    | Що                                  |
| ------------------------------------------ | ------------------------------------- |
| `feat(database): mock catalogue recipes` | 6 страв + 15 продуктів, `pnpm db:seed` |

Після міграцій у `recipes` було нуль рядків: довідники засіяні, страв немає,
і список рецептів у застосунку порожній. Шість мок-страв закривають цю дірку
до появи імпорту в адмінці.

**Сід, а не міграція.** Довідники (категорії, кухні, дієти, 15 quick-pick
продуктів, FAQ, тарифи) — частина продукту, тому вони в міграціях. Ці шість
страв — риштування: міграція зробила б їх вічними на кожному середовищі.

- **Ідемпотентність через фіксовані id.** У `recipes` немає natural key, тож
  id захардкожені й починаються з `5eed`. Повторний запуск бачить рядок і
  пропускає — перевірено, другий прогін дав `created 0, already present 6`.
  Той самий префікс робить прибирання одним рядком:
  `delete from recipes where id::text like '5eed%'` (страви перед
  продуктами — `recipe_ingredients` тримає їх через `ON DELETE RESTRICT`).
- **Продукти шукаються за англійською назвою, а не створюються наосліп.**
  Вісім із інгредієнтів — це ті самі quick-pick'и з міграції; сліпий insert
  поклав би другі «Помідори» в пошук продуктів. Перевірено: дублікатів 0,
  quick-pick'ів так само 15, усього продуктів 30.
- **Макроси страви рахуються з композиції.** Колонка зберігається, не
  виводиться на читанні (так задумано — імпортовані рецепти приносять власні
  числа), але для мок-даних розрахунок `Σ(per100g × amount/100)` — єдиний
  спосіб, щоб цифра на картці збігалася зі списком інгредієнтів під нею.

Покриття для фільтрів: 6 категорій, 3 кухні (+3 страви без кухні), 9 різних
дієтичних міток, 32 звʼязки крок↔інгредієнт.

---

### Зріз 18 — скрипт деплою ✅

| Коміт                                | Що                                     |
| -------------------------------------- | ---------------------------------------- |
| `feat(infra): deploy script with health verification and rollback` | `infra/prod/deploy.sh` |

`git pull` → збірка трьох образів → `IMAGE_TAG` у `.env.prod` → міграції →
`up -d` → перевірка `/health/ready` → відкат на попередній тег, якщо не
піднялося. Прапорці: `--no-pull`, `--no-cache`, `--tag <sha>` (деплой уже
зібраного тега — він же ручний відкат).

- **`set -o pipefail` + `grep` без збігу вбиває скрипт мовчки.** Ловилося на
  `IMAGE_REPO`, який необовʼязковий: `grep` виходить з 1, pipefail валить
  пайплайн, `set -e` завершує скрипт **посеред присвоєння і без жодного
  повідомлення**. Знайшлося тільки тому, що прогнав гарди по-справжньому —
  `bash -n` таке не бачить. Тепер у `read_env` є `|| true`.
- **`up -d` порівнює посилання на образ, не вміст.** Перезбірка під тим самим
  тегом лишає контейнер на старому шарі. Тому теги = git sha, а брудне робоче
  дерево скрипт відхиляє: тег, що не відповідає своєму sha, непридатний для
  відкату.
- **Відкат повертає контейнери, а не схему.** Міграції односторонні й уже
  застосовані — скрипт це прямо друкує, замість вдавати, що відкотив усе.

---

### Зріз 19 — self-hosted Actions runner ✅

| Коміт                                          | Що                                       |
| ------------------------------------------------ | ------------------------------------------ |
| `ci: deploy from a self-hosted actions runner` | `.github/workflows/deploy.yml`, runbook, `--ref` у `deploy.sh` |

Як на 11am: раннер стоїть **на тому самому сервері**, куди деплоїмо, а тіло
job'а — один рядок `deploy.sh --ref development`. Уся логіка лишається в
скрипті, тож ручний деплой по SSH і автоматичний виконують той самий код.
Друга реалізація в YAML розійшлася б із першою, і виявилося б це під час
інциденту.

- **Ніякого `actions/checkout`.** Раннер щоразу робить чисту робочу теку, а
  деплою потрібен канонічний клон — там `.env.prod` і локальна історія
  образів, без якої немає відкату. Шлях приходить змінною `PROD_REPO`
  (variable, не secret: секрет не видно в логах, і помилку в шляху було б
  неможливо діагностувати).
- **Токен Actions не дає git-доступу** поза робочою текою раннера, а
  `deploy.sh` робить `git fetch` саме в канонічному клоні. Потрібен окремий
  deploy key на читання — крок 3 у runbook'у.
- **Job висить, а не падає**, якщо раннера немає: «Waiting for a runner to
  pick up this job» — єдиний симптом, і GitHub про нього не сповіщає.
- `--ref` у `deploy.sh` робить `fetch` + `checkout -B`, а не `pull`: CI називає
  гілку, і сервер може стояти на іншій — або на коміті, який хтось відкотив
  руками.

Попередження GitHub про self-hosted раннери стосується **публічних**
репозиторіїв, де форк-PR виконав би довільний код на вашому залізі. Цей
приватний.

---

## Далі

### Поза цим планом

Наступні кандидати:

- **`meal-plan` FR-006** — кошик на страві й «Додати до списку покупок»
  тепер можна добудувати: домен списку є. Це невеликий додаток до плану, а
  не окремий зріз.
**Клієнтський API закрито.** Лишилися дві специфікації без серверної
частини: `system/error-screen` (її взагалі немає — форма звернення вже
існує) і `onboarding/intro-slides` (18 FR, усі клієнтські).

Наступні кандидати:

- **Перевірка чека проти Apple і Google** — єдине, що відділяє підписку від
  `Implemented`. Потрібні ключі.
- **Що саме дає підписка.** Зараз нічого: жоден ендпоінт її не перевіряє,
  бо специфікація не каже, що перестає працювати без неї. Це продуктове
  рішення, не інженерне.
- **Admin-зрізи** — їм спершу потрібні специфікації: у `docs/specs/admin/`
  зараз лише README, а адмінка вже викликає ~8 ендпоінтів, яких немає ніде.
- **Підключення мобілки до живого API** — окремий трек, тепер уже проти
  повного набору доменів.

Підключення мобілки до живого API — окремий трек.

---

## Свідомі відхилення від плану

- **`@dns/utils` не створено.** У плані був, але жодного спільного хелпера
  ще не знадобилось — створимо разом із першим, а не наперед.
- **`packages/database` тепер залежить від `@dns/constants`.** Резолюція
  мови потребує `DEFAULT_LANGUAGE`, і одна константа в двох пакетах
  розійшлася б першою. `constants` не має runtime-залежностей, тож ціна
  нульова.
- **Редагування власної страви (`PATCH /recipes/{id}`) не збудовано.**
  Специфікація `create-dish` явно виносить його за межі, але кнопка
  «редагувати» в макеті деталі є і поки веде в нікуди.
- **Власна страва не має категорії.** Форма її не питає, тож власні страви
  не потрапляють під фільтр категорій. Вигадати категорію за людину
  означало б поставити страву в добірку, якої вона не обирала.
- **Імена зовнішніх ключів у Postgres обрізаються на 63 байти, мовчки.**
  `recipe_step_ingredients.recipe_ingredient_id` давав ключ на 68 символів;
  колонку скорочено до `ingredient_id`. Перевіряти довжину перед тим, як
  міграція піде далі за локальну базу.
- **Адмінка ще не знає про ADR-0006.** `GET /admin/recipes/ingredients/all` має
  стати запитом до продуктів, а редактор складу — вибором продукту. Правка в
  `apps/web`, не лише в API; робиться разом з admin-зрізом.
- **OAuth-верифікацію не перевірено проти живих Apple/Google.** Токен провайдера
  неможливо видобути в тесті, тож у db-специфікаціях підмінено рівно цей крок
  (`FakeOAuthVerifier`), а вся логіка create-or-link виконується по-справжньому.
  Перед релізом потрібен ручний прогін із реальним пристроєм і заповненими
  `GOOGLE_CLIENT_ID` / `APPLE_CLIENT_ID`.
- **`ADMIN_PERMISSION_MATRIX` ще немає.** ADR-0003 його визначає, але
  споживач з'явиться в admin-зрізі; додамо тоді ж.
- **OTP зберігається в БД, а не в Redis.** У 11am код лежить у Redis-кеші й
  ліміту спроб не має. ADR-0003 вимагає лічильник спроб (5), тож
  `OtpService` лишається без стану (генерація + bcrypt-хеш), а рядок,
  строк дії та лічильник належать auth-домену (`otp_codes`).
- **Збірка через webpack, а не tsc.** `paths` вказують на `packages/<pkg>/src`
  поза текою застосунку, тож tsc виводить `rootDir` у корінь монорепо і
  кладе точку входу в `dist/apps/client-api/src/main.js`. Webpack-білдер
  (як у 11am) дає плаский `dist/main.js`: workspace-пакети бандляться,
  решта `node_modules` лишається зовнішньою.
- **`packages/CLAUDE.md` послаблено** для `@dns/constants`: пакет тепер
  явно може імпортувати `@dns/shared-types` (types-only, нульова
  runtime-залежність), інакше він не міг би типізувати власні значення.

## Відомі борги

- **Видалення акаунту нічого не видаляє — потрібне рішення.** Запит і
  скасування працюють, завдання-прибиральник — ні, і причина не в обсязі
  роботи: спершу треба ухвалити
  [ADR-0005](docs/adr/0005-what-account-deletion-erases.md) (`Proposed`), бо він
  упирається в строки зберігання фінансових записів. Проміжна ручна процедура є
  і відпрацьована на локальній базі:
  [`execute-overdue-account-deletions`](docs/runbooks/execute-overdue-account-deletions.md),
  запускати щотижня. Сигналу від моніторингу немає — тихе накопичення
  невиконаних запитів на видалення нічим себе не виявляє, а це юридична
  проблема.
- **Прострочені рядки ніхто не прибирає.** `refresh_tokens` зберігає й спожиті
  токени (це і робить крадіжку помітною), `otp_codes` і
  `password_reset_permits` лишаються після закінчення строку. Потрібне
  періодичне прибирання — cron або runbook.
- **Redis піднятий, але не використовується.** Сховище throttler'а — у
  пам'яті процесу: ліміти скидаються при рестарті й не діють між репліками.
  Для одного інстансу прийнятно; перед горизонтальним масштабуванням
  потрібен Redis-адаптер `@nestjs/throttler`.
- `GET /health` рахується в глобальний ліміт 60/хв. За кількох одночасних
  проб (LB + моніторинг) може почати віддавати 429 — тоді виносимо його
  з-під throttler'а.
- На старті обох сервісів двічі друкується попередження
  `Unsupported route path: "/api/v1/*"` від `LegacyRouteConverter`
  (Nest 11 + Express 5, middleware nestjs-pino). Автоконвертація спрацьовує
  коректно, шум некритичний.
- `pnpm lint` червоний через `apps/web` — 8 помилок
  (`react-hooks/set-state-in-effect`, `react/no-unescaped-entities`).
  Перевірено: рівно ті самі 18 проблем є і на `development`, тобто це
  успадкований борг, а не наслідок цих змін. Не чіпав.
- `.claude/skills/web/{architecture,data-layer}/SKILL.md` досі описують
  доступ через `supabase` з `@dns/api` — суперечать правилу «API-first» у
  root `CLAUDE.md`. Не чіпав: це не топологія бекенду, потрібне окреме
  рішення щодо веб-скілів.
- `.claude/knowledge/**` описує V1 на Supabase. Оновлюється подомено після
  кожного зрізу — правило вже в `docs/CLAUDE.md`.
- Веб-адмінка викликає ~8 ендпоінтів, яких немає ні в knowledge, ні в
  жодній специфікації (`/admin/favorites/*`, `/admin/support-messages/*`,
  `/admin/notifications/*`, `/admin/users/:id/block`, `/admin/languages`,
  `/admin/upload/recipe-image`). Їх доведеться проєктувати з коду адмінки —
  і спершу написати для них специфікації.
- `apps/web` ще не переведено на :3001 і не знято префікс `/admin` з
  дев'яти `*.api.ts` — робиться разом із першим admin-зрізом, щоб не
  ламати адмінку раніше, ніж з'явиться бекенд.

---

## Пастки середовища

- **Windows: `curl -d` у Git Bash псує кирилицю на `?` ще до відправки.**
  Виглядає точно як баг сервера — знаки питання приходять у відповіді й лежать
  у базі. Для тіл із не-ASCII писати JSON у файл і слати `--data-binary @file`.
  Те саме з `psql` у консолі Windows: у виводі `?`, хоча в базі коректно —
  перевіряти через `length()` або кодпоінти. Розгорнуто в
  [`apps/client-api/CLAUDE.md`](apps/client-api/CLAUDE.md).
- Той самий ефект псує heredoc-скрипти з кирилицею й тире у цій же консолі —
  довгі україномовні файли надійніше писати редактором, а не через `cat <<EOF`.
- **Не запускати `pnpm format` на весь монорепо.** Prettier переформатував
  ~470 файлів у `.claude/` і `docs/`, які до зрізу не мають стосунку.
  Форматувати цілеспрямовано: `pnpm --filter <pkg> exec prettier --write <шлях>`.
- **`TRUNCATE … CASCADE` у тестах змивав засіяні продукти.** CASCADE
  спорожняє кожну таблицю з зовнішнім ключем на ціль **незалежно від
  `ON DELETE`**, тож каскад від `users` забирав із собою 15 продуктів
  міграції. Тепер `DELETE FROM users` — правила дотримано: власні страви
  каскадують, користувацькі продукти відчіпляються, засіяне лишається.
- **`TaskStop` на `npx nest start` не вбиває сам сервер.** Оболонка гине,
  node-процес лишається слухати :3000, і наступний смоук мовчки опитує
  **стару збірку** — виглядає як «ендпоінт не працює». Перевіряти
  `Get-NetTCPConnection -LocalPort 3000` і гасити процес за `OwningProcess`.
- **Не ганяти смоук і `test:db` одночасно.** База одна, і `beforeEach`
  тестів чистить `users` під смоуком, а смоук лишає рядки під тестами.

- **Не застосовувати міграції, поки йде `test:db`.** DDL бере блокування на
  тих самих таблицях, і суїт зависає замість падати — я так втратив прогін на
  50 хвилин. Це ширший випадок пастки «одна база»: під час суїту базу не
  чіпати ні смоуком, ні `db:migrate`.

- **`http2 on;` — це nginx 1.25.1+.** На старішому це «unknown directive», і
  `nginx -t` падає **цілком**, забираючи з собою всі чужі vhost'и на машині.
  Робоча форма — `listen 443 ssl http2;`, саме та, що в конфігах 11am. Мораль
  ширша: dev-сервер уже обслуговує інші проєкти, тож `nginx -t` перед
  `reload` — і ніколи `restart`, поки тест не зелений.

- **`docker build` тягне контекст репозиторію.** `apps/mobile` — гігабайти
  Expo-виводу, тож `.dockerignore` викидає його цілком і повертає лише
  `package.json`: без маніфесту `pnpm install --frozen-lockfile` падає з
  «lockfile is not up to date», хоча `--filter` його й не ставить.

## Як перевірити локально

```bash
docker compose up -d           # postgres:16, redis:7, minio + бакет
cp .env.example .env
pnpm install
pnpm db:migrate
pnpm typecheck                 # 10 воркспейсів — зелено
pnpm dev:client-api            # :3000, Swagger /docs
pnpm dev:admin-api             # :3001, Swagger /docs
```

Тести:

| Команда                                      | Що                                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------ |
| `pnpm --filter @dns/constants test`          | 16 — Atwater, Mifflin-St Jeor, добові норми і БЖВ                                    |
| `pnpm --filter @dns/validation test`         | 53 — плюс каталог, власна страва, вікно плану, дата і одиниці списку покупок         |
| `pnpm --filter @dns/api-common test`         | 4 — форма `ApiError`, 500 без витоку                                                 |
| `pnpm --filter @dns/api-infrastructure test` | 11 — коди та перевірка власності файлів                                              |
| `pnpm --filter @dns/client-api test:db`      | 187 — плюс підписка і реферали (треба docker)                                        |

Наскрізний прогін auth (перевірено вручну, `OTP_DEV_CODE=000000`):

| Крок                                   | Результат                                                        |
| -------------------------------------- | ---------------------------------------------------------------- |
| `POST /auth/register`                  | 201, код у логах stub-клієнта                                    |
| `POST /auth/verify-email`              | 200, пара токенів                                                |
| `GET /auth/me` з Bearer                | 200 з id/email/emailVerifiedAt                                   |
| `GET /auth/me` без токена              | 401                                                              |
| `POST /auth/refresh`                   | 200, нова пара                                                   |
| повтор старого refresh                 | 401 — і виданий між ними токен теж мертвий (ланцюжок відкликано) |
| невалідне тіло                         | 422 з `errors[{path,message}]`                                   |
| permit на `/auth/me` і `/auth/refresh` | 401 в обох випадках                                              |

`docker compose` цього разу піднімався: міграція застосована, 5 таблиць у
базі, db-тести проходять.
