# dependencies
node_modules/
.pnpm-store/

# build outputs
dist/
.next/
*.tsbuildinfo

# env files
.env
.env.local
.env.*.local

# Claude Code — personal overrides + scratch
CLAUDE.local.md
**/CLAUDE.local.md
.claude/settings.local.json
**/.claude/settings.local.json
.claude/mcp.json
# Isolated checkouts for parallel agents; each is a full working tree.
.claude/worktrees/
# Points at a port on 127.0.0.1 that only this machine's IDE is listening on.
.mcp.json

# IDE
.vscode/
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Expo
apps/mobile/.expo/
apps/mobile/ios/
apps/mobile/android/

# test
coverage/

# Vercel
.vercel

# Playwright MCP — local browser session traces / screenshots / console logs
.playwright-mcp/
**/.playwright-mcp/

# scratch
temp/

# NestJS build output
apps/*/dist

# Deployment secrets — the compose stacks read these, they never go in git.
infra/prod/.env.prod
infra/prod/.env.obs

# Локальний скретч: конфіги й дампи з сервера, не для репозиторію.
tmp/
